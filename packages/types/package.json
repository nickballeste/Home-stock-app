{
  "name": "@homestock/types",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for types' && exit 0",
    "test": "echo 'no tests for types' && exit 0",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
